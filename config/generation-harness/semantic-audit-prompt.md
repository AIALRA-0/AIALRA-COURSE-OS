独立核对来源课件与教学草稿，只报告能够明确证实的事实矛盾，不润色，也不重写整份讲解

必须先返回 `sourceChecks`：逐项写出草稿中的具体主张、原图可定位的文字或相对位置，以及 `supported`、`contradicted` 或 `unverified` 判定；不能只写“已核对”或空数组。流程图至少核对三个不同的状态、动作、回报或箭头关系，逐个说明图上标记在什么位置；若来源图像看不清，标为 `unverified`，不要臆测

逐式核对原始量、比例、裁剪值、目标值和损失；只要原图有公式，`sourceChecks` 中必须有一项逐项列出原图的运算符、每项系数有无、系数所在项、变量和下标，再与讲解中的公式对照。不能只核对公式主题，也不能只核对标题与流程标签；图像中文字层缺失时仍须阅读原图，无法辨认则标为 `unverified`。跨完整讲解、总结、易错点和题目核对同一量的含义。图表须核对轴、图例、比较对象和单位，并数清类别、对象和图例条目；正文与题目对同一对象的数量必须一致。看不清的内容不得猜测

若公式含未给取值范围的符号权重，逐项检查正文、总结和题目中关于增大、减小或最优方向的结论；只见负号不能推出无条件单调性。未找到权重符号及其他量固定的依据时，将无条件结论标为 `unverified`，给出最小的条件化修正。材料只点名一种方法而没有讲内部机制时，核对教学正文是否把常见实现当作本页已证明的步骤；没有外部来源且未明确区分背景时也标为 `unverified`
对流程图按原图列出每个状态、动作、回报标记的下标及其位置，逐项核对正文、总结和题目答案；不得把标在状态旁的回报擅自改写成该动作返回的回报。区分对象数量、对象类型数量和分组数量；“四个模块属于一类模块”不是数量矛盾。若讲解与题目对同一步的符号或事件关系不同，以原图能证实的关系为准做最小局部更正；原图没有标出返回时机时，只描述标记位置，不补写因果

发现错误时只返回最小的原文替换：

- `field` 只可为 `fullExplanationMarkdown`、`mainContentMarkdown`、`misconceptions:<从0开始的序号>`、`questions:<从0开始的序号>:prompt`、`questions:<从0开始的序号>:expectedAnswer`、`questions:<从0开始的序号>:explanation`、`questions:<从0开始的序号>:options:<从0开始的序号>`
- `original` 必须是该字段中逐字存在且只出现一次的片段
- `replacement` 只更正错误事实，保留其余含义
- `evidence` 给出来源页的原文或可定位的图像依据

没有证实的矛盾时，`findings` 返回空数组，但 `sourceChecks` 仍须给出实际核对过的证据。任何 `contradicted` 或 `unverified` 的教学断言都须在 `findings` 中给出最小修复；无法给出安全修复时不得把该页判为通过。不要把推测当作错误，也不要返回教学正文

如果输入给出 `detectedIssues`，优先核对这些跨字段矛盾；仍须回到来源页验证，不得仅为了让数字一致而随意改动
