写作策略快照：{{WRITING_POLICY_SNAPSHOT_ID}}
任务契约：GENERATE + TEACHING
用户提供语言：{{LANGUAGE}}
用户提供质量模式：{{QUALITY_MODE}}
阅读单位：独立教学页面
页面编号：{{PAGE_NUMBER}}
页面标题：{{PAGE_TITLE}}

系统随后提供 Resource Package、Requirement Package、Rule Package 和 Teaching Blueprint

先在内部完成下面的工作，不把过程标签写进正文：

1. 判断页面类型和信息密度
2. 列出页面全部有效对象，区分正文、公式、图形、表格、代码、页眉页脚和装饰
3. 把每个有效对象分配到一个主要讲解位置
4. 确认读者理解每个对象需要的最小前提
5. 根据依赖关系安排讲解顺序，删除重复解释
6. 核对每个 atom 和 requiredFields 确实有正文证据

正文必须自适应本页内容，不得套用固定六段标题。短页面不要被扩写成空泛长文，复杂页面不能因为控制篇幅而漏掉公式、图形、代码、数字、条件、否定或边界

Resource Package 中的 pageKind 和 sourceDensity 同时决定篇幅。封面不超过 900 个中文字符，目录不超过 1800 个中文字符，稀疏页不超过 2000 个中文字符，普通页不超过 3500 个中文字符，密集技术页不超过 5000 个中文字符。不要讲解页码、配色、空白、装饰线或“页面没有出现的对象”，除非这些信息改变内容含义

普通中文正文、标题和列表项不得使用中文句号，行末不得保留中文分号。首次出现专业术语时使用“中文全称（English Full Name）：定义”或“缩写 中文全称（English Full Name）：定义”，在同一个连续块中讲清是什么、干什么、怎么干、何时使用以及如何区分。英文名词、短语和缩写不能脱离中文名称单独出现

学习正文不得出现内部策略名称、哈希、模型、成本、审核、验证、来源类型或覆盖状态。缺少来源时只说明当前材料无法确认的具体内容及其影响

离线提取的来源内容：

{{SOURCE_TEXT}}

原始页面图像是第一手来源。按页面自然阅读顺序核对文字、公式、箭头、图表和图片区域；离线文字只辅助检索，冲突时不得静默猜测
