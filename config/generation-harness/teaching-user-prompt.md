写作策略快照：{{WRITING_POLICY_SNAPSHOT_ID}}
任务契约：GENERATE + TEACHING
用户提供语言：{{LANGUAGE}}
用户提供质量模式：{{QUALITY_MODE}}
阅读单位：独立教学页面
页面编号：{{PAGE_NUMBER}}
页面标题：{{PAGE_TITLE}}

可信前页来源，仅供建立承上启下的关系，不能当成本页原始事实：
{{PREVIOUS_PAGE_CONTEXT}}

系统随后提供本页的来源内容、覆盖要求与讲解安排；这些是写作依据，不是要写进学习页面的标题

先在内部完成下面的工作，不把过程标签写进正文：

1. 判断页面类型和信息密度，若自动分类与原图不符，以原图为准
2. 列出页面全部有效对象，区分正文、公式、图形、表格、代码、页眉页脚和排版资源；只有来源结构、样式与上下文能共同证明纯排版时才不为它写教学正文，原件仍须保留；有效替代文字、内容型链接和加载失败资源不能直接当装饰
   对流程图中的状态、动作、回报和其他带下标的标记，先按图上位置逐个建立对应关系；标记在某个状态旁边，不等于材料已经说明它由前一步或后一步返回
3. 把每个有效对象分配到一个主要讲解位置
4. 确认读者理解每个对象需要的最小前提
5. 根据依赖关系安排必要的承上启下、先验知识、具体目标、完整讲解和简短总结；一个事实只在正文完整解释一次。先验知识已经用“名称：定义”写过的术语，完整讲解只写它在本页的具体作用、对象关系和推导，不再以同一个名称和冒号重复定义
6. 核对每个 atom 和 requiredFields 确实有正文证据

正文必须自适应本页内容，不得套用固定六段标题。短页面不要被扩写成空泛长文，复杂页面不能因为控制篇幅而漏掉公式、图形、代码、数字、条件、否定或边界

本页讲解安排中的 pageKind 和 sourceDensity 只给出篇幅上限，不是凑字数目标。封面不超过 900 个中文字符，目录不超过 1000 个中文字符，稀疏概念页不超过 2000 个中文字符；稀疏但含公式、流程图、表格或代码的页面，以及普通页，必要时可写到 3500 个中文字符；密集技术页不超过 5000 个中文字符。目录只讲材料本身给出的层级与阅读顺序，不抢先为后续主题补写定义。不要讲解不改变内容含义的页码、配色或纯排版元素，也不要罗列“页面没有出现的对象”

普通中文正文、标题和列表项不得使用中文句号，行末不得保留中文分号。首次出现专业术语时使用“中文全称（有依据的英文名称）：定义”或“缩写 中文全称（有依据的英文名称）：定义”，在同一个连续块中讲清是什么、干什么、怎么干、何时使用以及如何区分。学术概念可依据可靠学术来源核对通用英文表达；没有对应证据时不编造英文，也不声称它没有英文名称。英文名词、短语和缩写不能脱离中文名称单独出现；正式名称内部含缩写时就近展开

公式变量也必须放在合法的 `$...$` 或独立公式块中，不能输出 `W_e$v_i;v_j$` 这种混杂写法；数学定界符不完整时先根据原图核实，不能靠猜测修补。逐项核对原图公式的正负号、系数有无、系数所在项、变量、下标与运算顺序；原图首项没有系数时不得为了形式对称补出一个系数，读不清的符号不要编造

学习正文不得出现内部策略名称、哈希、模型、成本、审核、验证、来源类型或覆盖状态。缺少来源时只说明当前材料无法确认的具体内容及其影响

离线提取的来源内容：

{{SOURCE_TEXT}}

原始页面图像是第一手来源。按页面自然阅读顺序核对文字、公式、箭头、图表和图片区域；离线文字只辅助检索，冲突时不得静默猜测
数清图中的对象，并区分“几个对象”“几类对象”和“几组对象”；正文、总结和题目须保持同一对象及符号下标的一致性，拿不准标记与事件的先后关系时只描述图上可见位置
