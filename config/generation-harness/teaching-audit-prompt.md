本轮只检查教学和表达，不重新判定事实是否逐字存在于当前页。事实核验由独立步骤负责，正确背景定义与可推导步骤不因为原图没有逐字展示而删除
前页来源 previousPageContext 可证明承接关系；按 writingRules 和 compositionContract 原文核对，不增加额外风格要求。保留正确的数字、条件和原始对象；只返回 teachingChecks 与精确最小 findings，不返回 sourceChecks
字段必须使用输出结构中的实际路径，原文必须来自该字段

读取每个字段的完整字符串，不能用自己输出或摘录的长度推断原文截断。声称定义或题目解析未完成时，先核对该字段实际结尾；findings.original 必须是输入中的完整连续片段。易错点中的错误观点、选择题干扰项与被保留的原始错误公式必须结合纠正说明判断，不能把用于反驳的文字判成作者结论

检查各字段的教学与写作合同：

- 读者入口：承上启下是否用日常中文交代本页问题，是否提前使用了未解释的专业名称
- 承接去重：chapterBridgeMarkdown 非空时，完整讲解开头不得再次复述“上一页”或“前一页”；正文标题应直接命名技术对象或关系，不使用“页面上的对象”一类审计标题
- 术语：所有普通英文，包括单个单词、定义内部术语、表格外说明和题目，必须配中文；原表原值与真实代码保持原样，引用英文后必须讲清中文含义，不能只加引号规避翻译
- 名称证据：中文括号内只放与中文概念有对应依据的英文名称本体；产品、组织和规范核对官方名称，学术概念可核对可靠来源中的通用表达；未查证、查证失败、含义歧义和确实没有适用英文名称不得混为一谈；正式名称内部的缩写也须就近展开，首次定义后优先只用中文

- 可见对象：图表与有效按钮标签说明所指对象及判断范围；内容型链接只有核对目标页后才说明其实际内容；纯排版资源需要来源结构、样式与上下文依据，不能仅凭外观删除原件或捏造教学段落
- 前提：先验知识是否给出理解正文所需的定义、用途、机制、条件和区别，是否用陌生概念循环定义
- 独立项目：逐步计算、多个目标、多个比较对象、表格列说明应按层级逐项分行；一个术语定义或同一因果过程保持连贯，不机械拆分
- 表格：逐列说明名称、单位或范围与作用，逐行对应原对象；原表并不等于已经有了中文解释
- 讲解：读者能否逐步从输入走到中间量再到结果，关键结论的条件是否在总结和答案里丢失，示范能否复算
- 叙述视角：正文是否直接讲对象与关系；若“页面给出”“本页列出”“表中写着”“原图显示”反复充当主语，应删除多余来源旁白，只在原始标签、来源冲突和证据边界处保留
- 论证强度：多个竞争目标不能单独推出“不存在唯一最优解”，方法存在局限不能自动推出“这一代不够用”；绝对结论必须有目标函数、权重、约束、实验或其他直接证据
- 目标兑现：逐项比对学习目标与正文结论；正文证明多个参数同时改变时，目标不得声称另一参数不变，目标承诺不变时必须有公式、计算或来源直接支持
- 易错点：误解、原因、正确判断与核对方法分别成段，原因必须解释实际关系
- 题目：包括每个选项的数学定界符、题干、答案与解析；独立计算步骤分别列出，不能只重述答案

上述缺口在 findings 中给出原文精确片段和最小必要替换，evidence 说明规则与该段的具体缺失，禁止以“清晰”“已覆盖”等自评词代替定位；原来正确的数字与限定条件必须完整保留

findings 是可执行补丁，不是意见清单：同一字段中的 original 不能彼此重叠，replacement 必须一次修完该片段内指出的问题；英文原图标签需要保留时，replacement 同时保留原标签并紧接中文含义，不能只删标签或只加引号；已经满足规则的字段不得为了统一措辞重写

如果本次是 TEACHING_STYLE_RECHECK，只检查当前修复后的字段；已经补齐中英文、分段、对象关系或题目解析的项目必须判为 supported，不能沿用上一次审计的旧结论


另返回 teachingChecks，按 entry、terms、prerequisites、structure、objects、reasoning、questions 七项逐项给出 verdict 和 evidence，不得遗漏或重复：

- entry：日常中文入口和前页关联
- terms：所有字段的双语名称、首次定义与后续中文使用
- prerequisites：读者理解下一步所需前提及定义完整性
- structure：独立项目、段落和层级，包括表格外解释和易错点
- objects：来源对象逐项有讲解，原表与原图的有效元素不遗漏
- reasoning：原因、步骤、可复算示范、结论条件和跨字段一致性
- questions：全部题干、选项、答案、解析的内容和数学

evidence 必须指出实际检查的字段和正文片段，以及为何足够或缺少什么；没有表格或前页时据实指出对象不适用，并检查当前实际对象，不编造表格。任一不满足项必须为 contradicted 或 unverified，并在 findings 中提供可安全定位的修复；无法安全修复时保留失败，不把“规则存在”当成内容已遵守

任何 contradicted 或 unverified 的 teachingChecks 必须同时提供实际 field 和该字段中的连续原文 quote；quote 逐字保留 Markdown、数学定界符、空格和标点。无法找到原文位置时不能给负面结论，必须重新读取完整字段；不得凭印象声称内容重复、缺失、截断或未翻译
