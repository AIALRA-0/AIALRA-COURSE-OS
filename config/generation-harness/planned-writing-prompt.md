你正在把一页课件编写成能够独立阅读的高质量教学页面
任务契约：GENERATE + TEACHING；课件图片和提取文字是 SOURCE，教学计划只用于安排讲解顺序，不能替代或修改来源事实
请求末尾提供当前写作技能入口、格式规则、解释框架和公式规则的完整原文；先完整阅读，再按适用规则写作；不要把内部规则名写进正文
保留现有栏目分工：先验知识补必要前提；学习目标从已知对象说明可完成的动作；完整讲解由浅入深解释本页；主要内容只压缩结论；易错点辨析真实误解；四道题检验理解
一次写出所有主体栏目，不把开头、讲解和总结拆成多次生成；chapterBridgeMarkdown 暂时输出空字符串，前页承接稍后添加，不在本次请求里等待前页
优先解释页面实际给出的对象和关系，必要时给能复算的例子；短页不扩写成缺失清单，技术页不能漏掉公式、图表、代码、条件和有意义的标签
逐式说明公式用途、所有变量和组分、运算顺序、结果含义及适用条件；图表按原对象逐项解释；代码说明输入、状态变化、输出和失败条件
课件公式按页面原式解释；背景公式只有在帮助理解本页时才补充，并说明其适用前提及与原式的区别；课件未声明等价或简写时，不推断原式漏项、写错或只是简写
页面特有的数字、数量、表格单元格、标签、箭头和图中关系，只有在原图或提取文字中逐项核实后才能写成课件事实；矩阵值按行列定位复核。数字标签未注明单位或所计对象时，只保留原标签，不猜测其所计对象；标题和封面不构成后续章节内容或方法的证据。通用知识可以作为背景解释，不能冒充原图观察；无法确认的值应说明不确定，不能补成确定事实
若视觉转写同时给出总数和逐项列表而二者矛盾，只根据可定位的逐项列表讲解，不转述冲突的总数或把识别矛盾写进教学正文。若转写没有明确给出某个矩阵单元格或最大值，不补写具体数值或极值
先验知识每项以准确的中文知识点名称和有依据的英文名称开头，遵守当前技能的术语定义形式；不能核实英文时不猜译
完整讲解的标题按内容分层，段落和独立项目按语义换行；只有独立公式居中，正文和含行内公式的列表左对齐
易错点每项包含加粗的“错误理解：”“错因：”“正确判断：”“核对方法：”四个分段标签，错因解释为什么错，不只给结论
每页恰好两道理解题和两道四选一选择题；选择题答案逐字匹配选项，答案解释写出推理与条件
题目要求逐字回忆课件标题或文字时，答案照录原图的大小写、标点和引号，不增删标点或自行添加引号
不要把计划、来源类别、模型、成本、校验状态或覆盖账目写入学习正文
coverageEvidence 只作兼容字段；没有可信证据时输出空数组，不为页码、文字行或格式账目编造讲解
只返回符合最终 TeachingPackage 结构的对象；内容事实以课件为准，格式以当前写作策略为准
